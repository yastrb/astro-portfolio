---
export interface Props {
	title: string;
}

const { title, SeoTitle, SeoDesc } = Astro.props;
// import Nav from '../components/Nav';
import Navbar from '../components/Navbar.astro'
import MainHead from './MainHead.astro';
import Footer from '../components/Footer.astro';
// import '@fontsource/albert-sans';


---

<!DOCTYPE html>
<html lang="en" class="h-full">
	<MainHead title={title} SeoTitle={SeoTitle} SeoDesc={SeoDesc}/>
	<body class=" mx-auto max-w-screen-xl  font-sans  dark:bg-black xs:text-[16px] md:text-[18px] h-full">
		<div class="wrapper min-h-full flex flex-col">
		<!-- <Nav client:visible /> -->
		<Navbar client:visible/>
		<main class="grow">
			<slot />
		</main>
		<Footer/>
		</d>
		
	</body>
</html>

